fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
python-dotenv>=1.0
pydantic>=2.0

# --- Proveedor de IA por defecto: Gemini (capa 100% gratuita) ---
google-genai>=1.0.0

# --- Proveedores de IA alternativos (opcionales, instala solo el que uses) ---
openai>=1.30        # AI_PROVIDER=openai o AI_PROVIDER=groq (API compatible)
# anthropic>=0.34    # AI_PROVIDER=anthropic

# --- Integración opcional con Google Sheets (guardado automático de fichas) ---
# gspread>=6.0
# google-auth>=2.30
